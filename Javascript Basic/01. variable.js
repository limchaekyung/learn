// 변수 선언
// 변하지 않는 값은 const, 변할 수 있는 값은 let으로 선언
/* 
1. 변수는 문자와 숫자, $와 _만 사용
    const MY_HOME = "...";
    let _ = 1;
    let $ = 3;
2. 첫글자는 숫자가 될 수 없음
    let 1stGrade = 'A+'; (X)
3. 예약어는 사용할 수 없음
    let let = 99; (X)
4. 가급적 상수는 대문자로
    const MAX_SIZE = 99;
5. 변수명은 읽기 쉽고 이해할 수 있게 선언
    let a = 1; (X)
    let userNumber = 3; (O)
*/

/* const name = "Mike"
const age = 30;

alert(age) */
